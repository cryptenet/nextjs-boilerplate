import tw from 'twin.macro';
import { styled } from 'stitches.config';

const Brand = styled('div', {
  ...tw`bg-cryptenet-logo bg-no-repeat bg-center bg-contain w-1/2 h-1/6`,
});

export default Brand;
